# Company Dashboard

A responsive React + Vite dashboard displaying company information using React Mosaic.  

---

## Run Locally

1. Clone the repo:

```bash
git clone https://github.com/yuriinagorniak/synergy-way_test-task.git
cd company-dashboard
```

2. Install dependencies
npm install

3. Run development server
npm run dev


Build Production
npm run build
npm run preview

Docker
1. Build the Docker image
docker build -t test-task .

2. Run the container
docker run -p 8080:8080 test-task
